export const signInDoctor = async (email, password) => {
  // Firebase sign-in logic will be implemented here later
  return { success: true, userId: 'dummyDoctorId' };
};

export const registerDoctor = async (email, password, fullName) => {
  // Firebase registration logic will be implemented here later
  return { success: true, userId: 'dummyDoctorId' };
};
